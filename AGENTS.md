# Donna-Data-Visualizer

## 项目简介
根据表格文件xls、asc、csv绘制出曲线图。最后的目标是通过pyinstaller对整个项目进行封装，使得可以在其他电脑上独立运行。

## 技术栈
- python虚拟环境：donna

## 项目文件夹结构
- `data/`：可用的测试数据
- `build/`：运行pyinstaller中途产生的文件
- `dist/`：通过pyinstaller打包好的最终文件
- `icon/`：应用程序的图标
- `docs/`：项目计划方案

## 关键约束
- 回答和md文件使用中文，代码中使用英文
- 禁止更改`Donna/`之外的文件
- git版本控制由我手动执行
- 进行测试等活动一定使用donna虚拟环境，禁止更改或使用其他的虚拟环境
- 需要写入大段内容时，分段写入防止字数超出限制和超时
- 对项目进行改进时，如果有之前的代码，直接在现有的代码上修改；如果没有，则结合项目文件结构新建文件
- 程序压缩为zip由我运行